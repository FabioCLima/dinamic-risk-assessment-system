# Configuration Profiles

- `config.dev.json`: profile mapped to the current `workspace/` implementation.
- `config.prod.example.json`: example layout for a cleaner production-style repository.

These files are intended to document path strategy for the portfolio version.
